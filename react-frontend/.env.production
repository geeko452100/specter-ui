# Loaded automatically by `vite build` (mode=production by default).
# Not a secret — just the public API hostname — so this is safe to commit,
# unlike react-frontend/.env for local dev (gitignored, see .env.example).
VITE_API_BASE_URL=https://api.specterui.dev
